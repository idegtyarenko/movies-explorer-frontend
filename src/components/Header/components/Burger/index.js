import IconButton from "ui/IconButton";

import "./Burger.css";

export default function Menu() {
  return (
    <IconButton
      className="burger except-desktop"
      type="button"
      ariaLabel="Меню"
    />
  );
}
